name: Release (deprecated)

on:
  workflow_dispatch:

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecated workflow notice
        run: |
          echo "This workflow is deprecated."
          echo "Use .github/workflows/electron-release.yml (electron-builder) instead."
